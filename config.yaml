model:
  default: "minimax-m2.7:cloud"
  max_tokens: 4096

temperatures:
  drafter: 0.4
  gap_finder: 0.9
  overreach_finder: 0.9
  judge: 0.3

loop:
  max_rounds: 10
  cases_per_agent: 3

session_dir: "sessions"
